{
  "name": "ghostvault",
  "version": "2.5",
  "description": "GhostVault v2.5 - Streaming encrypted file storage",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/sync-version.mjs && cd core && npm run build && cd ../apps/web && npm run build",
    "dev": "node scripts/sync-version.mjs && cd apps/web && npm run dev",
    "lint": "eslint . --ext .ts,.tsx",
    "test": "vitest",
    "generate-docs": "node scripts/generate-docs.mjs",
    "generate-ai-db": "node scripts/generate-ai-db.mjs",
    "sync-version": "node scripts/sync-version.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0",
    "eslint": "^9.0.0",
    "prettier": "^3.3.0",
    "vitest": "^2.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
